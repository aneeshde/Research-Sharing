<%= form_for(@user_faq) do |f| %>
  <% if @user_faq.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_faq.errors.count, "error") %> prohibited this user_faq from being saved:</h2>

      <ul>
      <% @user_faq.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Question %><br />
    <%= f.text_area :Question %>
  </div>
  <div class="field">
    <%= f.label :paper %><br />
    <%= collection_select :user_faq, :paper_id, Paper.all, :id, :title%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
